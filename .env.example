# MatchMiner - local environment configuration.
#
# Copy this file to ".env" and fill in your own values.
# install.bat creates .env from this template automatically if it is missing.
# .env is git-ignored, so your real credentials are never committed.

# --- PostgreSQL connection -------------------------------------------------
# Full connection URL used by Django (parsed by dj-database-url).
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/matchminer

# --- Django ----------------------------------------------------------------
# Secret key for cryptographic signing. Use a long random string for real use.
DJANGO_SECRET_KEY=change-me-to-a-long-random-string

# Verbose error pages. Keep True for local development; set False in production.
DJANGO_DEBUG=True

# Local HTTP development. Set True when serving over http://localhost:8000 so the
# session/CSRF cookies are sent over plain HTTP (Secure cookies are not), which
# is required for login to work locally. Leave unset/False in hosted HTTPS envs.
DJANGO_LOCAL_HTTP=True

# --- Stealth browser (itftennis family) ------------------------------------
# The itftennis circuits scrape www.itftennis.com (behind Imperva/Incapsula)
# through a patchright Chromium. These knobs are all optional — sensible
# defaults are picked per-OS — but can be overridden here.
#
# Headless vs headed. Headless WORKS (it solves the Incapsula challenge), and is
# the default on Replit/Linux. On Windows the default is headed (you see the
# Chrome window). Set True to force headless on Windows too (lighter, no window),
# or False to watch the browser locally while debugging.
SCRAPER_BROWSER_HEADLESS=True
# Browser channel. "chrome" uses your real Google Chrome (most stealthy; the
# Windows default); leave blank to use the bundled/Nix Chromium (Replit default).
# SCRAPER_BROWSER_CHANNEL=chrome
# Per-request rotation: a fresh browser + proxy IP per tournament. Default True.
# When on AND Scraper.threads > 1, phase 2 launches that many browsers at once.
# SCRAPER_BROWSER_ROTATE_PER_REQUEST=True

# --- Scraper credentials ---------------------------------------------------
# Ioncourt (college dual matches) logs into api.ioncourt.com with a phone number
# + password. Fill these in to enable that scraper; leave blank and the Ioncourt
# run fails honestly (every other scraper is unaffected). Never commit real
# values — .env is git-ignored.
IONCOURT_PHONE=
IONCOURT_PASSWORD=
