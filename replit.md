# MatchMiner

MatchMiner is a Tennis Intelligence Platform SaaS that delivers daily, AI-scored tennis insights mined and ranked from across the web.

> Note: the artifact directory/slug is still `permitlify` (the app started as "Permitlify" before the rebrand). The user-facing brand is **MatchMiner**. Renaming the slug/directory would break the workflow and proxy paths, so it is intentionally left as-is — only the title, wordmark, domain text, and logo are MatchMiner.

## Stack (Django rebuild)

The app was rebuilt from scratch in **Django** (Python), replacing the previous React+Vite frontend and the Express API. Django now serves every page and handles authentication directly.

- Python 3.11, Django 5.2
- DB: PostgreSQL (Django ORM). Users in `auth_user`, sessions in `django_session`. App data in `accounts_scraper` (the seeded tennis source — currently just Billie Jean King Cup), `accounts_run` (per-run history, logs, and three CSV outputs: items data, requests telemetry, errors telemetry), and `accounts_proxy` (the user-managed proxy pools).
- Server: gunicorn (production), `manage.py runserver` (development)
- Static: WhiteNoise (`CompressedManifestStaticFilesStorage`)
- Config helpers: `dj-database-url` (reads `DATABASE_URL`), `psycopg2-binary`
- Scraper HTTP client: `curl_cffi` (Chrome TLS impersonation, `use_cffi=True` parity), routed through an optional per-scraper proxy. Needed because the upstream ITF/Stadion API sits behind CloudFront, which 403-blocks datacenter/cloud IPs.
- Python deps are managed with `uv` into `.pythonlibs`; `python3` resolves there.

## Run & Operate

The app runs via the `artifacts/permitlify: web` workflow (do not run `pnpm dev`). All commands below run from inside `artifacts/permitlify/` (the workflow's working directory **is** the artifact dir):

- Dev server: `python3 manage.py runserver 0.0.0.0:$PORT --noreload` (wired in `artifact.toml`)
- `python3 manage.py migrate` — apply migrations (creates `auth_user`, `django_session`, etc.)
- `python3 manage.py createsuperuser` / `manage.py shell` — manage users
- `python3 manage.py collectstatic --noinput` — gather static files (prod build step)
- Required env: `DATABASE_URL` (Postgres). Optional: `DJANGO_SECRET_KEY` (falls back to `SESSION_SECRET`), `DJANGO_DEBUG` (default `True`; set `False` in prod), `DJANGO_LOCAL_HTTP` (default `False`; set `True` for local HTTP — see below).

### Local Windows dev (`bat_files/` helpers)

For running the app on a local Windows machine, the **`bat_files/`** folder (at the workspace root) holds numbered double-click helpers, run in order: `0_install.bat` (venv + `pip install -r requirements.txt` + copies `.env.example`→`.env`), `1_migrate.bat`, `2_collectstatic.bat`, `3_run_server.bat` (`runserver 0.0.0.0:8000`). Because they live one level down, each uses `%~dp0..` to reach the workspace root (root `.venv`, `requirements.txt`, `.env`/`.env.example`) and `cd`s into `artifacts\permitlify`. `requirements.txt` (root) mirrors `pyproject.toml`'s deps for the pip flow. Config comes from a root `.env` (git-ignored; template is `.env.example`) loaded by `settings.py` via `python-dotenv` (`load_dotenv` of both the artifact dir and workspace root, **non-overriding** so real Replit env vars always win — a no-op when no `.env` exists). **Key gotcha:** the app's session/CSRF cookies are `Secure`+`SameSite=None` for the Replit iframe, and Secure cookies are never sent over plain `http://localhost:8000`, so login would fail locally — set `DJANGO_LOCAL_HTTP=True` (the `.env.example` default) to switch them to non-Secure `Lax`. This var is unset on Replit, so the hosted preview keeps the secure cross-site cookies.

## Where things live

- `artifacts/permitlify/` — the Django project, previewPath `/`.
  - `manage.py` — Django entrypoint.
  - `matchminer/settings.py` — settings (DB, cookies, proxy, static). See "Replit integration" below.
  - `matchminer/urls.py` — root URL conf (includes `accounts`).
  - `accounts/` — the app:
    - `models.py` — `Proxy` (name/`kind` choices: residential·datacenter·mobile·isp/`address` (optional, may carry credentials)/`is_active`/`created_at`; `display_address` property masks the password for safe UI rendering); `Scraper` (slug/code/name/tour/domain/vendor_url/description/returns/`tournaments` JSON/`mode`/`maintenance_message`/`proxy` FK→Proxy SET_NULL related_name=`scrapers`/`threads` PositiveSmallInt default 5 — the scrape worker-thread count, editable in the Lab's Settings tab; class consts `THREADS_MIN`=1/`THREADS_MAX`=16/`THREADS_DEFAULT`=5 + `worker_count` property clamps it to [1,16]; `trigger_token` CharField (unique, the per-scraper schedule-webhook bearer secret — auto-assigned in `save()` when blank via `generate_trigger_token()`=`secrets.token_urlsafe(32)`; `rotate_trigger_token(save=True)` regenerates it; treat as sensitive — never log it)); `Run` (uuid, FK `scraper`, `launched_by` FK→user SET_NULL, tournament, date_from/to, status incl. `RUNNING` + `STOPPED` (user-force-stopped), `pid` (PositiveInt null — the background worker's OS PID, set by the worker itself via `os.getpid()` in its first save **and** by `_launch_run` from `Popen.pid`; used by the Stop button / reaper to SIGKILL the worker), started/finished, duration_ms, row_count, output_size_bytes, `log_text`, `csv_data` (items), `requests_csv`, `errors_csv`; helper props `is_maintenance`/`short_id`/`duration_label`/`size_label`/`has_csv`/`has_requests`/`has_errors`/`request_count`/`is_running`); and `RunLogLine` (FK `run`, `seq`, `level`, `text`) — one streamed log line per run, written incrementally by the background worker so the live console (and concurrent viewers) can poll. **Do not** add `run_count`/`last_run_at` as model props — they're query annotations. `Run` has a **partial unique constraint** `uniq_running_run_per_scraper` (`status='running'`) enforcing at most one in-flight run per scraper.
    - `runs.py` — now only holds the `ALL_TOURNAMENTS` constant used by views as the `Run.tournament` label. **All simulated/demo/seeded run generators were removed** — every run is a real live scrape.
    - `live_scrapers/` — real scrapers + their telemetry. `_stadion.py` is the shared, parameterised `curl_cffi` (Chrome-impersonation) port of the production ITF/Stadion team-competition spider — matching the production `use_cffi=True` client (direct or via the scraper's proxy; processes **all** discovered ties concurrently with a `ThreadPoolExecutor` whose worker count is `run_obj.scraper.worker_count` (per-scraper `Scraper.threads`, default 5, clamped 1–16; set in the Lab → Settings tab) — **no per-run tie cap**, so a full season is scraped (e.g. BJK Cup 2026 ≈ 478 rows) in ~1.5 min, well inside the 20-min reaper; 60-col Title-cased ITF CSV header; `sanitize_cell` injection guard). It exposes `StadionConfig` + `run(config, run_obj, log)` returning the 5-tuple `(items_csv, requests_csv, errors_csv, row_count, status)`. `_build_proxies(scraper, log)` honours `run_obj.scraper.proxy`: it returns a `curl_cffi` `proxies` dict (`{http, https}`) **only** when the proxy is active with a non-empty address (scheme defaulted to `http://`); otherwise `None` (direct). The proxies dict is threaded through `_get_json`/`_build_row`, and every GET uses `impersonate="chrome"` + the Chrome UA. The proxy **address is never logged** (may carry credentials) — only its name/type. **The upstream API is behind CloudFront, which 403-blocks datacenter/cloud IPs (incl. Replit's), so a residential/working proxy MUST be assigned (Lab → Settings) or the scrape returns 0 rows — exactly why production routes through `settings.PROXIES` with `use_proxy=True`. Verified: BJK Cup 2026 via an assigned proxy returns 478 rows in ~1.5 min.** `billiejeankingcup.py` (Fed Cup, draw code `bjkc`, women) is a thin wrapper: a `CONFIG` + `def run(run_obj, log): return _stadion.run(CONFIG, run_obj, log)`. `_stadion.py` stays parameterised (`StadionConfig` — draw code / gender / match URL), so sibling ITF/Stadion team competitions (e.g. Davis Cup, draw code `dc`, men) can be re-added later as more thin wrappers. `telemetry.py` — `Telemetry` (lock-guarded, since ties are scraped concurrently) records every HTTP request (`record_request`) and error (`record_error`) and emits `requests_csv()` / `errors_csv()` whose columns/formats match the user's production framework (requests: `request_id`=uuid4, `duration`="{ms:.0f} ms", `fingerprint`=sha1(url+status+size), `http_method`, `response_size`="{n} bytes", `http_status`, `last_seen`="%Y-%m-%d %H:%M:%S UTC", `url`; errors: `timestamp`="%Y-%m-%d %H:%M:%S.%f", `level`, `message`, `exception`=traceback). Add new real scrapers here and register them in `run_scrape`'s `LIVE_SCRAPERS`. Real years are derived from `Run.date_from`/`date_to` (the year dropdown sets Jan 1–Dec 31 of one year).
    - `management/commands/run_scrape.py` — the **background worker**. `python3 manage.py run_scrape <uuid>` runs in its own OS process (launched via `subprocess.Popen`, `start_new_session=True`). Its `_RunLogger` persists each line as a `RunLogLine` (and prints it); it is **lock-guarded** so the concurrent scraper's worker threads keep `seq` monotonic/unique for the live-console poller. Dispatch is a `LIVE_SCRAPERS` registry (`billiejeankingcup`); each returns the 5-tuple, which the worker persists to `csv_data`/`requests_csv`/`errors_csv` + status/row_count/size/duration. **No simulated fallback**: an unregistered slug fails the run honestly (status `FAILED`, a `Telemetry` errors CSV explaining no scraper is wired, no fabricated rows). On finish it materialises `Run.log_text`; a broad `except` writes the traceback into both the log and an errors CSV and marks the run `FAILED`.
    - `migrations/0002_seed_scrapers.py` — inline idempotent data migration seeding the lone `billiejeankingcup` scraper. `migrations/0007_keep_only_bjkc.py` — trims existing DBs down to that one scraper (deletes the rest; their runs cascade via the `Run` FK). `migrations/0009_run_pid_alter_run_status.py` — adds `Run.pid` and the `STOPPED` status choice. `migrations/0010_scraper_trigger_token.py` — adds `Scraper.trigger_token` (AddField default `""` → RunPython backfill of unique tokens → AlterField `unique=True`).
    - `views.py` (`login_view`; `@login_required` `overview_view` / `scrapers_view` / `scraper_detail_view` / `scraper_run_view` / `run_events_view` / `run_log_view` / `run_log_download_view` / `run_csv_download_view` / `run_requests_download_view` / `run_errors_download_view` + POST-only `stop_run_view` (force-stop an in-flight run) + `@csrf_exempt` POST-only `scraper_trigger_view` (the **schedule webhook** — see the Schedule tab in Product) + real `proxies_view` (GET list/stats/add-form; POST add+validate / delete) + remaining placeholder pages; POST-only `logout_view`). `scraper_detail_view`'s POST branches on a hidden `form` field: `settings` saves `Scraper.proxy`+`Scraper.threads` (Settings tab), `schedule-rotate-token` calls `Scraper.rotate_trigger_token()` (Schedule tab), otherwise it saves Production/Maintenance status; its GET `tab=="schedule"` branch builds `trigger_url`/`secret_name`/`workflow_filename`/`default_year`/`workflow_yaml` (via module-level `_github_workflow_yaml`). Both `scraper_run_view` and the webhook share `_start_scraper_run(scraper,*,year,launched_by)` (raises `RunStartError` for maintenance/duplicate/launch) and `_parse_year` (`YEAR_MIN`=2000…`YEAR_MAX`=2030 → `date_from`=Jan 1 / `date_to`=Dec 31); the detail view passes `years` (descending) + `default_year` (current year, clamped) to the real-time tab. The webhook reads its token via `_extract_bearer_token` (`Authorization: Bearer` **only**) and compares with `hmac.compare_digest`. Helpers: `_launch_run` (Popen; also saves `Run.pid`=`Popen.pid`), `_terminate_run_worker(run, settle=0.2)` (SIGKILLs the worker's **process group** via `os.killpg` — `start_new_session=True` makes the worker its own group leader — then best-effort `os.waitpid(WNOHANG)` reaps the zombie; returns True when the worker was killed **or was already gone** (`ProcessLookupError`=success), False only when a live process couldn't be signalled), `_reap_stale_runs` (fails runs stuck `RUNNING` past `STALE_RUNNING_AFTER`=20min — now first calls `_terminate_run_worker(run, settle=0)` so a genuinely stuck worker is killed before the `RUNNING` lock is released), `_run_lines`/`_run_log_text` (live-rows-or-`log_text` fallback). `stop_run_view` (POST, route `run_stop`): only marks a run `STOPPED` when the kill succeeded **and** it's still `RUNNING` after `refresh_from_db` — if the worker already finalised itself it keeps that result; if the kill failed it leaves the run `RUNNING` (no fake STOPPED) and shows an error. **PID-clobber guard:** the worker (`run_scrape.py`) sets `run.pid=os.getpid()` in its first save and its **final** save uses explicit `update_fields` that **exclude `pid`/`started_at`**, so a late-finishing worker can never overwrite the PID (or silently undo a STOPPED/FAILED status set elsewhere). `urls.py`.
  - `templates/` — `base.html` (html shell), `app_base.html` (authenticated layout: left sidebar + topbar with `breadcrumb`/`topbar_actions` blocks + theme toggle + `tr[data-href]` row-click that ignores anchors; defines a `content` block), `login.html`, `overview.html`, `scrapers.html`, `proxies.html` (manage proxy pools), `scraper_detail.html` (the Lab — tabbed; real-time tab branches on `active_run`: live console + polling JS vs. start form; Settings tab = proxy dropdown + worker-threads number input), `run_log.html` (paginated log viewer), `_placeholder.html`, `partials/` (`logo.html`, `scraper_table.html`, `pagination.html`). The sidebar marks the active item via `active_nav`.
  - `static/css/styles.css` — brand tokens + `--app-*` theme tokens (light `:root` + `html[data-theme="dark"]`) + login layout + app/lab component styles. Append new component CSS using the `--app-*` tokens.
  - `static/favicon.svg` — preserved from the original app.
  - `.replit-artifact/artifact.toml` — repurposes the `web` artifact to run Django (see below).
- `attached_assets/dailypermit_*.html` — original supplied design mockups (source of truth for each page's look).

## Replit integration (important, non-obvious)

The Replit artifacts framework has no Python/Django kind, so the existing `web` artifact slot is repurposed by hand-editing `artifact.toml` (validated via `verifyAndReplaceArtifactToml`). Key facts learned:

- The workflow runs the `run` command with **cwd = the artifact directory** (`artifacts/permitlify`), not the workspace root. Use plain relative commands (`python3 manage.py ...`) — no `cd`, no path prefix.
- `verifyAndReplaceArtifactToml` **cannot change `integratedSkills`**, so the original `react-vite` `integratedSkills` block is kept byte-for-byte even though it's now Django (harmless metadata).
- `--noreload` is used so Django's file watcher doesn't choke on the monorepo.
- The preview is a **cross-site iframe**, so `settings.py`: omits `XFrameOptionsMiddleware`, sets session/CSRF cookies `SameSite=None; Secure`, sets `SECURE_PROXY_SSL_HEADER`, and trusts Replit domains in `CSRF_TRUSTED_ORIGINS`.
- Because cookies are `Secure`, test auth over the **HTTPS** dev domain (`$REPLIT_DEV_DOMAIN`); `curl` will not send Secure cookies over plain `http://localhost`.

## Auth & seed account

Django's built-in auth. Seeded login: username `salman` (the password was set out-of-band, not stored in the repo). Flow verified end-to-end: login → `/overview/`, protected route redirects to `/` when unauthenticated, wrong password shows an inline error, logout returns to `/`.

## Product

- **Login** (`/`) — two-column page: dark sales/marketing panel left (headline with blue→green gradient, proof cards, trust badges), white sign-in form right (username/password). MatchMiner-branded (tennis-ball logo, wordmark, domain). Complete.
  - Caveat: the left-panel sales copy and proof cards are still permit-themed from the original concept (faithful port). Rewrite for tennis when updating body content.
- **Authenticated app** — pages behind login share `app_base.html` (left sidebar + topbar). DB-backed.
  - **Overview** (`/overview/`) — greets the user; three live stat cards (active scrapers, runs today, in maintenance) + a "Recently active" scraper table.
  - **Scrapers** (`/scrapers/`) — lists the scraper(s) with Tour/Mode/Runs/Last-run (counts from query annotations) and an **Open lab** button → the detail page.
  - **Scraper Lab** (`/scrapers/<slug>/?tab=…`) — tabbed detail page (Real-time test, Calls history, Schedule, Settings, Status), the core feature:
    - **Real-time test** — runs a **real scrape as a background OS process**. The start form is a single **year dropdown** (2000–2030, default = current year); there are no date inputs. It POSTs to `scraper_run`, which validates the year is in range, blocks when in maintenance, creates a `RUNNING` `Run` (with `date_from`=Jan 1 / `date_to`=Dec 31 of that year), then `subprocess.Popen`s the `run_scrape` worker and redirects back to the real-time tab. When a run is in flight the tab shows a **live console** that polls `run_events` (JSON, `?after=<seq>` cursor) ~1s, escapes+appends new lines, auto-scrolls, and on `done` reveals the summary + Open log / Download log / **CSV** (items) / **Requests CSV** / **Errors CSV** / "Run another" (each download disabled if that file is empty, driven by `has_csv`/`has_requests`/`has_errors` in the events JSON). While a run is in flight the live console also shows a **runbar** (live dot + "Live · Run #x · PID n" + a **Stop run** button — a confirm-guarded POST to `run_stop`) that force-kills the worker process; on stop the run becomes `STOPPED` (grey pill) and the tab reverts to the start form. The log streams live **and** persists. Concurrency: at most one in-flight run per scraper (DB partial-unique constraint + friendly pre-check); a stuck/orphaned `RUNNING` run is reaped (failed, worker killed) by `_reap_stale_runs`, invoked from both the detail view and the events poller so a live console always terminates.
    - **Calls history** — paginated (12/page) list of runs: id, started, tournament, window, status pill, rows, size, duration, and per-run **Open log** (new tab), **Log** (.txt download), **CSV** (items), and **Requests** / **Errors** CSV downloads (shown when present). Routes are scoped by `uuid + scraper__slug` (no IDOR). Exactly one completed run lands here per real-time scrape.
    - **Schedule** — docs-only setup guide for scheduling this scraper via **GitHub Actions** (replaces the old workspace-level Schedule sidebar page, which was removed). Shows the per-scraper **trigger URL** + **trigger token** (masked, reveal/copy), numbered setup steps (add repo secret `MATCHMINER_<CODE>_TRIGGER_TOKEN`, commit `.github/workflows/<slug>-schedule.yml`, pick a cron, test via the Actions tab), the copy-ready workflow YAML (built in Python by `_github_workflow_yaml` to avoid Django `{{}}` collisions; `schedule` cron + `workflow_dispatch` year input), a curl example, and a confirm-guarded **Regenerate token** button (detail-view POST `form=schedule-rotate-token`). **The trigger endpoint** is the "handle" the Action calls: `POST /scrapers/<slug>/trigger/` (route `scraper_trigger`, `@csrf_exempt`, machine-to-machine) — reads the token **only** from `Authorization: Bearer <token>`, compares it with `hmac.compare_digest` (constant-time, never logged), and launches the **same** `_start_scraper_run` helper as the Real-time tab. Statuses: `401` bad/missing token, `400` invalid year (optional JSON/form `year`, defaults to current season), `409` a run is already in progress, `503` in maintenance, `201` started (JSON body: `run_id`/`run_uuid`/`status`/`year`/`events_url`). No GitHub connector/API is used — purely static docs + the webhook.
    - **Settings** — "Routing & performance": a dropdown to pick which `Proxy` this scraper routes through (or "Direct connection") **and** a worker-threads number input (1–16, default 5) → "Save settings", persisting `Scraper.proxy` + `Scraper.threads` (validated/clamped server-side). Active pools come from the Proxies page. The thread count controls how many ties are scraped in parallel (higher = faster but heavier on the proxy/upstream).
    - **Status** — Production/Maintenance radio + maintenance message, persisted to `Scraper.mode`; gates real-time runs. (The former **Code samples** and **Enhancements** tabs were removed.)
  - **Proxies** (`/proxies/`) — sidebar page to manage proxy pools: add as many as wanted (name + type residential/datacenter/mobile/isp + optional address), see counts by type and a table with masked addresses + "Used by" + delete. The per-scraper selection lives in each Lab's Settings tab. Addresses are rendered via `Proxy.display_address` (password masked).
  - **Run log viewer** (`/scrapers/<slug>/runs/<uuid>/log/`) — paginated (150 lines/page) log with metadata chips and Download log / CSV / Requests CSV / Errors CSV (the last three shown when present); opened via `target="_blank"`. Reads live `RunLogLine` rows while running, the materialised `log_text` snapshot once finished.

### Per-run CSV downloads (requests / errors / items)

Every run produces up to three CSVs, downloadable from the live console, Calls history, and the run-log viewer:
- **items** (`data.csv`, `Run.csv_data`) — the scraped rows; 60-col ITF schema with a **Title-cased** header (e.g. `Tie Id`, `Match Id`).
- **requests** (`requests.csv`, `Run.requests_csv`) — one row per HTTP call made during the run (see `telemetry.py` for exact columns/formats).
- **errors** (`errors.csv`, `Run.errors_csv`) — one row per failure (empty when a run had none).

### Real scrapers vs. the rest

The catalogue was deliberately trimmed to a **single** wired scraper — **Billie Jean King Cup** — to perfect it before adding more; other sources will be re-added later. It uses one public ITF/Stadion JSON API, but the endpoint sits behind CloudFront which 403-blocks datacenter/cloud IPs (incl. Replit's), so a **residential/working proxy must be assigned** in the Lab's Settings tab for it to return data (the HTTP client is `curl_cffi` with Chrome impersonation, matching production `use_cffi=True`). The shared `_stadion.py` engine is still parameterised (`StadionConfig`), so sibling ITF/Stadion team competitions (e.g. Davis Cup) can be re-added as thin wrappers; other source types would need the original framework's Selenium / AI extraction, which isn't available in this environment (`curl_cffi` IS now installed and is the scraper HTTP client). Any slug with no `LIVE_SCRAPERS` entry **fails honestly** (no fabricated data). Assigning a proxy (Settings tab) routes a scraper's HTTP traffic through that endpoint when an address is set, but it does **not** make an unwired scraper work — only a registered `LIVE_SCRAPERS` entry does.

## Legacy / cleanup

- `artifacts/api-server/` (Express) and `lib/api-spec/` are **no longer used** by MatchMiner — the Django app replaced them. They remain in the repo (and api-server's workflow still runs) but nothing depends on them. They can be removed on request.
- The old Express `users`/`session` Postgres tables are likewise unused; Django uses `auth_user`/`django_session`.

## User preferences

- Recreate the supplied designs faithfully/pixel-exact rather than improvising.
- Always push committed changes to the GitHub repo (`scrapelabs/Salman_Badr_New`, branch `main`) automatically after finishing work — do **not** ask each time. Fast-forward push only (never force); if histories ever diverge, stop and ask.

## Gotchas

- Run the app via the workflow, not `pnpm dev`. The workflow's cwd is the artifact dir.
- After model/settings changes, restart the `artifacts/permitlify: web` workflow. The server runs `--noreload`, so changes to Python (incl. `runs.py`, `run_scrape.py`, `live_scrapers/`) only take effect after a restart. **The `run_scrape` worker is a fresh `python3` process per run**, so it picks up worker-side code changes immediately — but the views that launch/serve it still need the restart.
- The worker shares the same `DATABASE_URL` as the web process (that's how live streaming works cross-process). Validate the whole flow with `django.test.Client` in `manage.py shell` (force_login + POST `{"year": …}` + poll `run_events`): it exercises the real subprocess + cross-process polling (real network calls to the ITF API) without the Secure-cookie/CSRF friction of `curl`.
- There is **no demo/seeded run data anymore** — runs are real live scrapes. Only `billiejeankingcup` is wired (the catalogue was trimmed to it); other slugs fail honestly. Don't reintroduce simulated runs.
- A `Proxy.address` may embed credentials (`user:pass@host`). **Never render the raw `address`** — always use `Proxy.display_address` (masks the password). Scrapers must **never log the address** — only its name/type. The `display_address` `re.sub` replacement uses a literal bullet char + `\g<1>`/`\g<2>` group refs (a `\u2022` escape in a regex replacement template raises "bad escape \u").
- Keep brand tokens global in `static/css/styles.css` `:root`; scope new-page CSS under that page's root class. App/lab styles use the `--app-*` light/dark tokens.

## Pointers

- See the `pnpm-workspace` skill for monorepo structure (note: this artifact is now Python, not a pnpm package).
